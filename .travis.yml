language: cpp

compiler:
    - gcc

script:
    - ./premake5 gmake
    # - make config=release_x64 tests
    # - ./bin/x64/release/tests
    - make config=release_x64 derplannerc
    # - ./bin/x64/release/derplannerc --out examples examples/travel.domain
    # - ./bin/x64/release/derplannerc --out examples examples/blocks.domain
    - make config=release_x64 example-travel
    # - make config=release_x64 example-blocks
    - ./bin/x64/release/example-travel
    # - ./bin/x64/release/example-blocks
