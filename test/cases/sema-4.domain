
domain test
{
    world { a(id32) b(id64) }

    task t1(x, y)
    {
        case( a(x) ) -> [ t2(x, y) ]

        case( ) -> [ t3(y) ]
    }

    task t2(x, y)
    {
        case( a(y) ) -> []
    }

    task t3(x)
    {
        case( b(x) ) -> []
    }
}
