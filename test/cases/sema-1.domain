//> error (11, 24): failed to unify type for 'x', current inferred type is type 'id64', new constraint is type 'id32'.
//> error (16, 24): failed to unify type for 'y', current inferred type is type 'id64', new constraint is type 'vec3'.
//> error (19, 13): failed to unify type for 'x', current inferred type is type 'id64', new constraint is type 'id32'.
//> error (28, 27): unbound variable 'z'.
domain test
{
    world { a(id64) b(id32) c(vec3) }

    task t1(x, y)
    {
        case( a(x) & b(x) ) -> []
    }

    task t2(x, y)
    {
        case( a(y) & c(y) ) -> []
    }

    task t3(x)
    {
        case( a(x) ) -> []

        case( b(x) ) -> []
    }

    task t4()
    {
        case( a(x) & (x < z) ) -> []
    }
}
