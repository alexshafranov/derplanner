//> error (9, 19): only constant expression is allowed in ':size'.
//> error (15, 37): variable 'X' isn't bound in all precondition conjuncts.
//> error (20, 33): only single attribute ':sorted' is allowed.
domain test
{
    fact { a(int32) b(int32) }

    fact c(int32) :size(1 + 1)
    fact d(int32) :size(1 + V)

    prim { p!(int32) }

    task t()
    {
        case( a(X) | b(Y) ) :sorted(X) -> []
    }

    task s()
    {
        case( a(X) ) :sorted(X) :sorted(X) -> []
    }
}
