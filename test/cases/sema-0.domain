//> error (11, 31): multiple definitions of 'a' found.
//> error (13, 15): recursive predicate 'p0' found.
//> error (15, 15): recursive predicate 'p1' found.
//> error (17, 15): recursive predicate 'p2' found.
//> error (19, 15): multiple definitions of 'p0' found.
//> error (27, 13): multiple definitions of 'p1' found.
//> error (31, 10): multiple definitions of 'hello' found.
//> error (33, 16): multiple definitions of 'x' found.
domain test
{
    world { a(int32) b(int32) a(int32) }

    predicate p0(x, y) = ( a(x) & p0(x, y) & b(y) )

    predicate p1(x, y) = ( a(x) & p2(x, y) & b(y) )

    predicate p2(x, y) = ( b(x) & p1(x, y) & p0(x, y) )

    predicate p0() = ()

    task hello()
    {
        predicate
        {
            p1(x) = ()
            p2(x) = ()
            p1(y) = ()
        }
    }

    task hello() {}

    task t1(x, x, y) {}
}
