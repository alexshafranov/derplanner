//> error (10, 15): mismatching number of arguments for 'a'.
//> error (15, 17): unbound variable 'x'.
//> error (20, 21): task 'prim!' is not declared.
domain test
{
    world { a(id32, id32) }

    task t1()
    {
        case( a(x, y, z, w) ) -> []
    }

    task t2()
    {
        case( d(x, y) ) -> []
    }

    task t3()
    {
        case() -> [ prim!() ]
    }

    task t4(x, y, w)
    {
        case( a(x, y) ) -> []
    }
}
